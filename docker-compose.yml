version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./src:/project/src
      - ./data:/project/data
    environment:
      - WATCHFILES_FORCE_POLLING=true
